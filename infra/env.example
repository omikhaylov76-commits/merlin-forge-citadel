# Шаблон. Настоящие значения — только в env Railway / локальном .env (в git не попадает).
DATABASE_URL=
MF_MASTER_KEY=            # только у оркестратора
RAILWAY_API_TOKEN=        # только у оркестратора
TELEGRAM_BOT_TOKEN=
TELEGRAM_OPERATOR_CHAT=
