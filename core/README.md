# core — ядро платформы
Единственный владелец Postgres. FastAPI. Внутри: домен (см. wiki/concepts/domain-model.md),
REST API для консоли/портала/ботов + internal API оркестратору (аренда jobs, ADR-0009),
RBAC+2FA, audit_log, биллинг HWM, outbox-события, jobs-очередь.
Не знает: как устроены движки, как деплоятся контейнеры (это оркестратор через jobs).
Заглушки v0: сервис не создан — только контракт границ. Ф0: скелет FastAPI + миграции 0001 + auth + аудит.
