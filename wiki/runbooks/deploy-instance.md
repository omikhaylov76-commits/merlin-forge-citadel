# Ранбук: деплой инстанса (каркас)
Консоль → Клиент → счёт → выбрать паспорт → «Развернуть»: ядро создаёт job, оркестратор
через драйвер поднимает сервис из образа типа бота с env по Контракту → ждёт первый heartbeat
≤120с → статус RUNNING. Откат: stop_close → удалить сервис → инстанс ARCHIVED. Всё в аудит.
