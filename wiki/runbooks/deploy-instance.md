# Ранбук: деплой инстанса (каркас)
Консоль → Клиент → счёт → выбрать паспорт → «Развернуть»: ядро создаёт job, оркестратор
через драйвер поднимает сервис из образа типа бота с env по Контракту → ждёт первый heartbeat
(таймаут — см. seams.md, строка «Бот не стартовал»: 5 мин от serviceCreate) → статус running.
Откат: stop_close → (по result=ok) teardown сервиса → статус stopped. Всё в аудит.
Полный жизненный цикл и машина состояний — flows.md; статуса ARCHIVED нет (терминальный — stopped).
