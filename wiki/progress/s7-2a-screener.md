---
type: progress
title: С7-2а — скринер по параметрам (импульс) — разовый подбор+скан
tags: [ф5-разведка, s7, screener, cartridge]
updated: 2026-07-16
---

# С7-2а — скринер по параметрам (возраст/оборот/ИМПУЛЬС)

Директива: список монет подбирается ПАРАМЕТРАМИ (не фикс-список). Возраст + оборот24h — это уже штатный
Этап A вендора (`universe.build_universe`: age_days из launchTime, low_turnover/young_listing). НОВОЕ —
импульс (всплеск объёма последнего 4ч-бара против среднего за N дней). cartridge-side, 0 vendor.

Гейт Куратора: ДО кода — план 5 строк в QUEUE (изоляция от штатного скана Галахада + RPS/бан) — ВЫЛОЖЕН.
Старт кода — на прямое «поехали» Оператора, ДО «ок» Куратора по плану (помечено в QUEUE, граница).

Последний коммит main: 5399139

- [x] 1. ОФЛАЙН-ядро `app/screener.py`: bars_for_days / volumes_of / impulse_ratio / has_impulse /
      select_candidates — чистые функции, без сети и без vendor. Тесты `tests/test_screener.py` (9):
      спайк/плоско/окно/края/None/порог k + разбор select_candidates. ruff чист, 118 тестов картриджа зелёные.
- [x] 2. Живой db-пайплайн `run_screener` + CLI `python -m app.screener` (build_universe → импульс из
      кэша 4h → scan_list на ОБА ТФ → отчёт). Изолированная SQLite (db_path), RPS=1. Запущен на прямое
      слово Оператора (отчёт-only). ⚠ Уточнение риска: скринер локально (IP Оператора) ≠ облачный
      Галахад (Railway IP) → живой скаут 403-баном НЕ задет.
- [x] 3. Первый живой прогон ЧИСТО (exit 0, 0 банов): вселенная 633→klines 120→Этап A 76
      (young_listing=31/low_turnover=15/short_history=12/wide_spread=5) → импульс×1.5 взято 4
      (BLUAI×4.5, HOME×3.8, ESPORTS×3.0, CRO×1.5), сетапов 4. Отчёт+JSON Оператору.
- [ ] 4. ОТКРЫТО (развилка доски, за Куратором): как показать результат на доске Разведки
      (отдельная БД+инстанс «скринер» A vs метка в БД Галахада B). Пока — отчёт-only, не льём.

Границы: cartridge-side, 0 vendor/ядро/боевое. Живой прогон Bybit — за гейтом (RPS≤1, малая вселенная
после отсева, НЕ параллелить с холодным Этапом A Галахада, следить за 403/backoff).
