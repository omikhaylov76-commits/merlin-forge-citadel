---
type: progress
title: MFC-001 — скелет ядра (core)
tags: [progress, mfc-001, core]
updated: 2026-07-10
---
# MFC-001 — план (ветка task/mfc-001)

Цель: живой каркас ядра, на который вешаются следующие фичи. FastAPI-app-factory, Alembic +
миграция 0001 (ФУНДАМЕНТ авторизации/аудита, НЕ весь домен), единый opaque-токен-механизм
(ADR-0008v2), /healthz, тесты (закон №8 — RBAC/auth), CI-гейт под §2. Границы контрактов уже
готовы (seams/flows/domain-model/ADR 0001–0011).

Границы (НЕ трогаю): код orchestrator/console/portal/bots; логика ключей биржи (Ф2); математика
биллинга (Ф3); таблицы instances/jobs/telemetry/billing (свои миграции при своих фичах, YAGNI);
импортов между модулями нет (закон №3).

Решения Куратора (утв. 2026-07-10): A миграция 0001 = только users/api_tokens/audit_log ·
B роль = STRING + CHECK (role IN ('operator','client')), НЕ нативный ENUM · C «часовой» отложен
в MFC-002 (пустой цикл нечем доказать) · D argon2 ТОЛЬКО пароль Оператора, opaque-токены (256 бит)
= SHA-256 (быстрый) · E env.example асимметричная пара с комментами. Append-only audit — гвоздь-тест.

Последний коммит: b8e8904
- [x] 1. Каркас: core/pyproject.toml (deps шага 1: fastapi/uvicorn/pydantic-settings + dev
        pytest/httpx/ruff), core/app (app-factory, config, структурный лог), /healthz.
        Доказано: uvicorn + curl /healthz → 200; ruff чисто; pytest зелёный. (f829070)
- [x] 2. Миграция 0001 (Alembic): users(role STRING+CHECK), api_tokens(token_sha256 …),
        audit_log(append-only через DB-триггер). + /readyz (БД+миграции на head) отдельно от /healthz.
        docker-compose.dev (pg16). Доказано: up/down/up чисто; pytest 5 passed (UPDATE/DELETE аудита
        отвергнуты, /readyz, /healthz); ruff clean. (c39ee9f)
        ⚠️ Dev-находка: локальный import psycopg на macOS вешал Gatekeeper — лечится
        `xattr -dr com.apple.quarantine .venv`; в CI (Linux) проблемы нет.
- [x] 3. Auth (ADR-0008v2): opaque-токен 256 бит → SHA-256 в БД, скользящий TTL, мгновенный отзыв;
        пароль argon2 (отдельно от токенов); TOTP-заготовка (pyotp, off); проверка владения на ВСЕХ
        ручках (SEC7); request-id → лог; login/logout → аудит. Миграция 0002 (users.email).
        Доказано: pytest 12 passed (login→me, revoke→401, RBAC→403, владение A→B 403, request-id);
        up/down/up чисто; ruff clean. Живой стек покрыт TestClient (uvicorn+curl упёрся в
        macOS Gatekeeper, поведение то же). (b8e8904)
- [x] 4. CI-гейт §2 — принесён вперёд в шаг 2 (c39ee9f): .github/workflows/ci.yml
        (Postgres + ruff + pytest на push/PR). Тест-каркас (alembic up/down, /healthz, /readyz,
        append-only) уже зелёный. Причина переноса — ненадёжный локальный Docker на macOS.
- [x] 5. Хвост: env.example → асимметричная пара MF_MASTER_PUBLIC_KEY (ядро) / MF_MASTER_PRIVATE_KEY
        (только оркестратор) + комменты (COH6); core/README под реальность; concepts/core-api.md
        + index + log. Вики-линт чист. → MFC-001 готов, на слияние task→main по §2.

Каждый под-шаг = свой коммит на ветке (доказанно-зелёное → ⛔ → commit → SHA сюда).
