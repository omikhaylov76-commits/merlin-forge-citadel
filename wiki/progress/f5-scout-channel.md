---
type: progress
title: Ф5 Разведка — scout-канал (дуга #50–#53) → Кузница
tags: [phase5, scout, contract, adr, schema, roadmap]
updated: 2026-07-16
---
# Ф5 Разведка · scout-канал (дуга #49–#53)

Цель дуги: довести живые сетапы скаута Пифагора до консоли (граф сетапа + сортировка), не заставляя
платформу сканировать рынок — она ПРИНИМАЕТ снимки от бота (ADR-0016 «Разведчик на борту, исправленный»).
Единица работы — директива #NN Куратора; каждый шаг — по одному, после приёмки предыдущего.

Последний коммит: 1d7e91c

## #49 — Рекон (данные сканера) — ГОТОВО (отчёт в QUEUE, 2026-07-16)
- [x] Экран Разведки в консоли (фикстура) + поля скаута КАК ЕСТЬ + канал данных + данные графика — сверены кодом.

## #50 — ADR-0016 + scout-канал Контракта v1 (ТОЛЬКО документы+схема+тесты, 0 строк в vendor/)
- [x] 1. ADR-0016 `wiki/decisions/0016-scout-channel-onboard-scout.md` — (а)–(д)
- [x] 2. Схема `contracts/telemetry-scout.schema.json` — replace-снимок per (instance,symbol,tf)
- [x] 3. Бамп Контракта v0→v1: $id/title 5 схем + CONTRACT_VERSION оба картриджа + стале-лынт доков
- [x] 4. Sync-гвозди оба картриджа (`tests/test_contract_version.py`): версия-синк + структурный гвоздь полей (pifagor)
- [x] 5. Зелёные тесты: paper-bot 23 · pifagor 73 · core 11 (схемы + schema↔Pydantic)
- [x] 6. Само-ревью (workflow 4 линзы) — красных нет; yellow-дельта внесена
- [x] 7. Приёмка Куратора построчно ✓ (развилка «Pydantic-зеркало» → #52 подписана) → merged в main (feat 74048a5 + fix ruff 1d7e91c), **CI зелёный = финал #50**.
  Разъезд «партитуры» (roadmap/progress/PROTOCOL) синхронизирован в этом же шаге (#50 ДОПОЛНЕНИЕ, принято).

**#50 ЗАКРЫТ (2026-07-16).** Стоп — жду директиву #51 (изоляция картриджа), сам не начинаю.

## #51 — Изоляция картриджа (В РАБОТЕ, доказано локально)
Последний коммит: c3f9c25 · механика в обёртке, 0 строк в vendor/, в облако НЕ катить, скаут на флоте НЕ включать.
- [x] fail-closed `SCOUT_ENABLED`-гейт в start.sh (vendor-дефолт True не решает; проверено (а))
- [x] отдельная `scout.db` через `DB_PATH` (env только дочернему скауту, не адаптеру; db.py:365 / ADR-0016 в.2; (б))
- [x] супервизор скаута в start.sh (liveness heartbeat + RSS-кап, рестарт только скаута) → `app/scout_health.py` (в)(г)
- [x] разведение бёрстов (SCOUT_RPS=1 / SCOUT_LIST_MAX=50 / SCOUT_CAL_UTC_HOUR / SCOUT_TFS) — env обёртки (в.5)
- [x] хвост A #48: динамический баннер по $LIVE_TRADING_ENABLED (dry-run ↔ LIVE demo)
- [x] доказательства (а)-(г) локальным dry-run (fake-scout, реальный start.sh через source-guard) + тесты 89 ✅
- [x] приёмка Куратора построчно ✓ (супервизор-трактовка + DATABASE_URL подписаны); merged f9988d6, CI зелёный
  🔶 DATABASE_URL → #52: скаут всегда на своей SQLite (зачистка DATABASE_URL дочернему скауту) + правка ADR-0016 в.2.

**#51 ЗАКРЫТ (2026-07-16).** Стоп — жду директиву #52 (труба целиком: ручка ядра + таблица/миграция 0009 + readout + адаптер scout_reader/mapper/push + сквозняк локально). Сам не начинаю. В облако не катим.

## #52 — Труба целиком (В РАБОТЕ)
Последний коммит: f9d3a94 · ядро+readout+адаптер+сквозняк локально. 0 vendor, консоль #53, SCOUT_ENABLED не выставлять, в облако не катить.
- [~] Ядро: ручка `POST /v1/telemetry/scout` (principal instance, SEC7) + Pydantic-зеркало `ScoutSnapshotIn` (полное, все required вкл. data_upto) + replace-семантика (upsert (instance,symbol,tf) + удаление выпавших) + капы 413
- [~] Миграция 0009 `scout_snapshots` (down_revision 0008_billing_lifecycle)
- [~] Readout `GET /v1/instances/{id}/scout` (require_role operator)
- [~] Адаптер: `scout_reader.py` (DB owner=False db_path=scout.db, триггер по scan_ts) + `mapper.py` (снимок+orders/position+config_mismatch 4 крутилки+detector_version/fingerprint/producer) + push в bot.py
- [~] Хвост #51: зачистка DATABASE_URL дочернему скауту + правка ADR-0016 в.2
- [x] Core sync-тест: ScoutSnapshotIn == схема (extra=forbid ловит дрейф, вкл. data_upto) + required-parity
- [x] сквозняк локально: fake-scout→scout_reader→mapper→push→ядро→readout; replace (WIFUSDT исчез)/идемпотентность доказаны; 413/зеркало в core-тестах. Локальный Postgres (homebrew, не docker).
- [x] само-ревью (workflow 3 линзы): 1 RED (триггер по scout_meta вместо scout_control — умершие сетапы висели бы до утра) ИСПРАВЛЕН + yellow-дельта. Тесты: core 32, cartridge 102, paper-bot 23.
- [x] коммит fa3f1b5 → **CI зелёный (7/7 джоб) = финал #52**; отчёт Куратору построчно в QUEUE

**#52 ЗАКРЫТ (2026-07-16).** Данные едут: scout → адаптер → ядро → readout.

### ХВОСТ #52 (приёмка — развилка №1 klines: РАСШИРИТЬ) — В РАБОТЕ
- [x] Схема: klines_tf enum → [4h,1h,15m,5m] + описание «= tf сетапа пока апстрим не даст младший ТФ» + Pydantic-зеркало (Literal) + examples
- [x] scout_reader/mapper: свечи скан-ТФ из кэша (build_scout_chart, ≤300 баров), klines_tf=tf сетапа
- [x] Тесты: снимок со свечами проходит зеркало+схему (core) + структурный гвоздь (картридж) обновлён
- [x] JSON-артефакт сквозняка `reference/scout-throughflow-b75bd17.json` (снимок с config_mismatch/levels/klines + ответ readout)
- [x] коммит 1e1afb8 → **CI зелёный (7/7)**. Тесты: core 33, cartridge 104, paper-bot 23.

**ХВОСТ #52 ЗАКРЫТ (2026-07-16).** Дальше — #53 (консоль-график Разведки: СО свечным фоном скан-ТФ + levels). Стоп — жду директиву #53. Облако/флот не трогаем.

**Форвард-риск #53 (зафиксировать при приёмке #53):** адаптер Фазы 1 НЕ шлёт klines (у скаута нет 15m/5m; ADR-0016 д). #53-график = **levels-only** (A→B + Fib + стоп линиями, без свечного фона) пока апстрим pifagor-v81 не отдаст младший ТФ. Живая верность orders/position — гейт go-live (в dry-run пусто).

## #53 — Живой экран Разведки (В РАБОТЕ)
Последний коммит: 138c87a · Scout.tsx с фикстуры на GET /v1/instances/{id}/scout. 0 vendor, ядро не трогать, SCOUT_ENABLED не выставлять, облако нет. **Строгий порядок: отчёт+скрины → приёмка → merge.**
- [x] api.ts getInstanceScout + TS-типы (зеркало scout-схемы); Scout.tsx с scoutFixture на live
- [x] селектор инстанса-представителя (дефолт = свежайший) + подпись «уровни от скаута {producer}»
- [x] канбан 4 колонки (Committed = производная position/orders); карточка (%-до-входа от last close «на закрытие», спарклайн, long)
- [x] деталь-вью Lightweight Charts (свечи + слой «теория» A/B/Fib/стоп + слой «факт» ордера/позиция + легенда) + разбор текстом
- [x] 4 обязательные плашки (возраст скана / config_mismatch / чей скаут / дисклеймер 4h-15m)
- [x] 3 разных пустых состояния (рынок тихий / скаут молчит / ошибка связи) + skeleton
- [x] сквозняк локально (Postgres homebrew + fake-снимки) + **7 СКРИНОВ** всех состояний; tsc+build чисты; 0 vendor, ядро/contracts не тронуты
- [x] отчёт QUEUE + само-ревью → **#53 КОД ПРИНЯТ построчно (приёмка часть 1)**: дизайн чист (тема слаш, 0 хардкода), типы==схема, логика/фиксы/трактовки ок
- [x] 7 PNG на диск `_curator/design/scout-screens/` (доска/деталь-график/mismatch/устаревание/рынок-тихий/скаут-молчит/ошибка) — для моста Куратора (часть 2 приёмки)
- [x] РЕНДЕР часть 2: дизайн/логика приняты; 🔴 блокер (график на 4 свечах) — ХВОСТ: fake-scout ~90 осмысленных 4h-баров (A→B→откат), обновлён `reference/scout-throughflow-b75bd17.json` (90 klines) + защита barSpacing в ScoutDetail (без нового кода экрана) + пересобран `2-detail-chart.png` (плотный график). tsc+build зелёные.
- [x] РЕНДЕР итерация 2: (1) рассинхрон 4-vs-90 свечей = кэш моста (данные/код верны — доказано: readout ядра 90 klines + console-лог «klines→setData: 90»); пересобран `2-detail-chart.png` свежий плотный. (2) фича «развернуть/свернуть» в ScoutDetail (модал max-w-1560 + график h-600, useState без localStorage, тема слаш) → `2-detail-chart-expanded.png`. tsc+build зелёные.
- [x] **#53 ПРИНЯТ целиком** (код+рендер построчно): 2 графика (обычный плотный + развёрнутый) ок; косметика (кап barSpacing только <15 баров) внесена перед merge; обычный кадр пересобран плотным.

**#53 ЗАКРЫТ (2026-07-16). ДУГА РАЗВЕДКИ #49–#53 ЗАВЕРШЕНА** — труба scout→ядро→консоль живая, Оператор видит сетапы с графиком. Дальше — Кузница (после отдельного решения). **Включение скаута на флоте = отдельное решение с Оператором, сам НЕ начинаю.**

## #54 — Включить скаут ВЖИВУЮ (на Галахаде, dry-run) — В РАБОТЕ
Решение Оператора: скаут на **ГАЛАХАДЕ** (dry-run, LIVE_TRADING_ENABLED=0), НЕ на боевом Персивале (гейт ADR-0016 (г) по IP-бану). Операционное включение флага + два пойманных бага.
- [x] `SCOUT_ENABLED=1` + `MF_SCOUT_PUSH=1` на сервис Галахада (Railway variableUpsert, только он; Персиваль+3 не тронуты) + передеплой → скаут поднялся 3-м процессом, своя scout.db, супервизор.
- [x] **Предблокер:** облачное ЯДРО было старше #52 (нет `/telemetry/scout`, нет таблицы 0009). Оператор одобрил → передеплой ядра на `:main` (миграция 0009 встала чисто, роут поднят). Труба замкнулась: `POST /telemetry/scout` = 202.
- [x] **Баг #51 (супервизор):** дефолт `SCOUT_GRACE_SEC`/`SCOUT_MAX_SILENCE_SEC`=180с < холодного Этапа A (300 монет @ RPS=1 ≈356с) → супервизор убивал скаут за stale в цикле; хуже — `max_silence(180) < SCOUT_BAN_SLEEP_SEC(600)` → штатный 403-backoff рестартил бы скаут в бан. Обойдено **операционно** (env Галахада: grace=1800, silence=1200). ⚠️ **Хвост: править ДЕФОЛТЫ в start.sh** (fleet-wide образ) — под отмашку Куратора, НЕ в этом заходе.
- [x] `PYTHONUNBUFFERED=1` на Галахада (env) → print() скаута flush'ится сразу (видимость Этапа A + 403 в реальном времени).
- [x] **Баг #52 (курсор пуша) — ИСПРАВЛЕН кодом (9b793f5):** курсор `max(last_a_ms, last_b_boundary_ms, scan_now_ack_ms)` — Этап A (без находок) давал пустой пуш и «съедал» курсор (last_a_ms=сейчас > округлённой last_b_boundary_ms) → находки первого Этапа B висели до след. 4h-границы (~2ч), утренняя рекалибровка обнуляла бы доску. Фикс: `_scan_cursor` = max(last_b_boundary_ms, scan_now_ack_ms), last_a_ms убран; чистку мёртвых сетапов несёт сам Этап B. **Это правка reviewed-логики #52** — +3 теста курсора; ruff+pytest(107) зелёные, CI 7/7. Куратору на заметку: амендмент к #52.
- [x] Скаут сканит настоящий Bybit: 730 инстр→626 перпов→klines 300→86 годных; Этап B за 99с нашёл **9 реальных сетапов** (4h: 6 [forming 3/ready 2/tracking 1] + 1h: 3 forming). После фикса передеплой Галахада → пуш сдвинулся на Этапе B → **readout = 9 снимков (2 ready, все с 300 klines)**. ETHUSDT/4h ready скор 100.
- [x] **2 скрина живой консоли** → `_curator/design/scout-screens/`: `live-board.png` (доска с реальными сетапами Галахада) + `live-chart.png` (ETHUSDT на настоящих 4h-свечах Bybit + слои теория/факт, факт=dry-run пусто). **403-банов 0, stale/rss-киллов 0** за весь прогон.

**#54 ДЕМОНСТРИРОВАН ВЖИВУЮ (2026-07-16):** труба scout→адаптер→ядро→консоль работает на настоящем рынке. Скаут остаётся включён на Галахаде — дальше решение Оператора (оставить/выключить/расширять). ⚠️ **Открытый хвост: дефолты grace/silence в start.sh** (сейчас обойдены env Галахада) — под отмашку Куратора. **Кузницу НЕ начинаю.**

Далее — Кузница (библиотека профилей + паспорта OOS + залок эталона).
