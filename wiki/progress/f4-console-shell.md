---
type: progress
title: Ф4 — консоль, первый шаг (дизайн-система + оболочка + Обзор)
tags: [f4, console, frontend, shadcn, slash]
updated: 2026-07-15
---
# Ф4 первый шаг (#32) · Консоль: дизайн-система + оболочка + экран Обзор

**Цель (#32):** развернуть консоль оператора; первый шаг — дизайн-система (слаш-токены как тема
shadcn) + оболочка (хедер+сайдбар) + экран Обзор → показать Оператору, затем раскатка по экранам.

**Стек:** Vite + React + TS + Tailwind v4 (CSS-first) + shadcn-паттерн (hand-rolled UI, без MCP).
Тема — слаш (`_curator/design/slash-tokens`). Консоль = дисплей поверх бэкенда Ф3 (деньги в ядре, #32).

**Что трогаем:** новый модуль `console/` (не пересекается с core/orchestrator/portal — границы CLAUDE.md §3).

Последний коммит: 840e7fa
- [x] 1. Scaffold console/ (package.json/vite/tsconfig/index.html) + npm install (91 пакетов)
- [x] 2. Дизайн-система: src/index.css (@theme слаш-токены → утилиты), Playfair+Inter, .gild/.tnum
- [x] 3. UI-примитивы (shadcn-паттерн): button/card/badge + states (пусто/грузится/ошибка) + useAsync
- [x] 4. Оболочка: Sidebar (навигация группами) + Header (крошка/поиск/СТОП ФЛОТ) + AppShell + роутер (11 экранов)
- [x] 5. Экран Обзор: hero AUM+P&L · 4 KPI · Капитал(кривая equity) · Требует внимания(машина состояний) · Здоровье · Лента
- [x] 6. Проверка живьём: typecheck чист, dev-сервер 200, скриншот Обзора + роутинг (клик→заглушка+крошка), 0 ошибок консоли

**Показано Оператору** 2026-07-15 (скриншот Обзора в «слаш»-виде).

## Дальше (раскатка по экранам, #32)
- Флот / Сделки / Клиенты + карточка клиента (на живом CRM-API Ф3).
- Кузница: ПОЛНЫЙ Конструктор (23 крутилки, скринер, матрица входа, OOS-паспорт) — по constructor-skeleton.
- Отчёты / Тревоги (сюда — readout `stuck-accounts`) / Настройки. Портал клиента (отд. поверхность).
- **Бэкенд-подзадача:** агрегат-эндпоинты флота (AUM/P&L/капитал-кривая/лента) → заменить фикстуры Обзора.
- **Инфра:** CI-джоба фронта (typecheck/build) — в Icebox/следующим; экран логина оператора.
- Спеки IA downstream-экранов запрошены у Куратора (QUEUE): console-*/kuznitsa-walkthrough/f3-crm-billing-spec.
