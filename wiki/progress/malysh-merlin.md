---
type: progress
title: «Малыш Мерлин» — эталон-нулевой-пациент (#11)
tags: [malysh-merlin, reference, pifagor, adr-0014]
updated: 2026-07-12
sources: [_curator/DIRECTIVES.md#11]
---
# progress: «Малыш Мерлин» (#11)

Цель (DIRECTIVES #11): две независимые страховки оригинала Пифагора @b75bd17 (v8.3) +
залоченный профиль-эталон + версионирование + ADR-0014. Оригинал pifagor-v81 не редактируем
(только неснимаемый тег-указатель + чтение). Реальные ключи/торговля — СТОП до go-live.

Последний коммит: a8db399

## Разведка (сделана)
- `config.knobs` @b75bd17: `KNOBS` (23 крутилки), `knobs.default(k)` (заводское из config-модулей),
  `KNOB_SPECS` (тип/границы/enum) → часть B = детерминированный экстрактор дефолтов, движок не гоняем.
- `reference/` отсутствует (создам). **git-lfs не установлен** → tar.gz в дерево через LFS не кладём.
- gh авторизован (omikhaylov76-commits), репо pifagor-v81 доступен; тега `malysh-merlin/*` ещё нет.

## Развилки — СНЯТЫ Куратором (#21, #22)
- **F1 — ОДОБРЕНО (#21):** архив tar.gz как GitHub Release-ассет Цитадели (в дерево не коммитим); SHA256 в
  reference/README. Плюс локальный клон Оператора = 3-я копия вне GitHub.
- **F2 — ОДОБРЕНО (#22):** Оператор явным словом подтвердил обе внешние записи (вариант 1): (a) тег+release
  на pifagor-v81; (b) release Цитадели с tar.gz-ассетом. Требование: SHA256 загруженного == reference/README,
  совпадение подтвердить в log/QUEUE. Секретов в release notes нет. Тег/релизы обратимы.

## План (под-шаги)
- [x] 1. Клон pifagor-v81 в scratchpad, `checkout b75bd17`, HEAD==b75bd176193abba1803899c092052c1f9c00a9eb ✓
- [x] 2. **B. Профиль-эталон:** экстрактор из чистого клона (env -i) → `reference/malysh-merlin-profile-v8.3.json`
      (23 крутилки × {value,type,границы} + metadata + инвариант). Значения сверены с комментариями knobs.py
- [x] 3. **A. Архив:** `git archive b75bd17 --prefix=…` → `malysh-merlin-v8.3-b75bd17.tar.gz` (1.5M, 493 файла);
      SHA256=`04f81c6196e76768ebf1eb6746a37d49a3f2668edc52c09a73d752ca8b5d4b34` (в scratchpad, не в дереве)
- [x] 4. `reference/README.md`: архив (коммит/SHA256/где) + профиль + 3 копии + инвариант
- [x] 5. **C/D. ADR-0014** + строка в index.md (log — на шаге 8)
- [x] 6. Коммит локальных артефактов (dd4b3db) — reference/ + ADR-0014 + index + progress
- [x] 7. **F2 (внешние записи, #22):** (a) тег `malysh-merlin/v8.3-b75bd17` в pifagor-v81 + release ✓;
      (b) release Цитадели + ассет tar.gz ✓; SHA256 загруженного == README (04f81c61…) ✓
- [~] 8. roadmap (#11 done) + log ✓ → merge ветки в main (--no-ff) → QUEUE «#11 готово к разбору»

## Границы
pifagor-v81 не править (только тег-указатель + чтение клона); архив/профиль — read-only; реальные
ключи/торговля — отдельный гейт go-live. Полный залок профиля в UI — Ф5 (сейчас только замороженный артефакт).

## #43 Шаг A — свидетельство мягкой посадки Персиваля (зафиксировано по DIRECTIVES #44)
Куратор (#44) принял локальный сквозняк клиента №0 «Персиваль» и попросил закрепить доказательства в
репо (в /tmp они эфемерны). Артефакт конфига — `reference/perceval-configdiff-b75bd17.json` (тот же
`reference/`, что и профиль-эталон; НЕ `_curator/evidence/`, т.к. `_curator/` git-игнорируется, а нужен
именно закоммиченный аудируемый артефакт). Режим: локально, dry-run, без облака/Docker/боевой торговли.

**Главное — конфиг-diff (аудит faithfulness):** 23 крутилки воркера под deploy-env Персиваля
(BYBIT_DEMO=1 · LIVE_TRADING_ENABLED=0 · ключи) == эталон «Малыш Мерлин» @b75bd17 **ТОЧЬ-В-ТОЧЬ,
`drift: []`**. Safe-mode env 23 крутилки НЕ трогает → воркер грузит код-дефолты = эталон.

**Транскрипт сквозняка S4 (контур Контракта Бота v0, реальным адаптером картриджа — не моком):**
- **heartbeat** 204 → инстанс `starting → RUNNING`, `health=ok` ✓
- **equity** идёт: 3 точки EquityPoint, equity `20000` (воркер сеян его же кодом: `Ledger.seed(10000,10000)`) ✓
- **pause** → `PAUSE_ENABLED=True` в config_state воркера + инстанс `paused` + ack ✓
- **stop_close** → `killswitch_active=1` + ack + картридж ВСТАЛ (процесс вышел) ✓
- **/fleet/overview** показал Персиваля живьём: `running=1 · clients=1 · AUM=20000` (stopped-инстанс
  корректно исключён из AUM — правка #40 ш.2 работает на РЕАЛЬНЫХ данных) ✓

**Честная граница:** реальный трейд-движок Пифагора локально НЕ гонялся (тянет Bybit-demo + рыночные
данные = Шаг B/облако). БД воркера инициализирована ЕГО ЖЕ кодом, дальше реальный адаптер гонял
S4-контракт → контракт и конфиг доказаны faithfully; наторгованной прибыли нет (её даёт Шаг B).

Аудит-трейс (локальная эфемерная БД): client `f03b9838…` · инстанс `0bbefc5d…` · профиль
`12e5fecd…` (Малыш Мерлин) · deploy-job `69ac6444…`.

**Шаг B (#45/#46)** — на операторском гейте: ghcr-образ ПРИВАТНЫЙ + registry-cred (PAT `read:packages`).
Не начат.
