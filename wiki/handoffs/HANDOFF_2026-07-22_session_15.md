---
type: handoff
title: HANDOFF 2026-07-22 session 15 — S8 F-warm-button ОТГРУЖЕН + живая постановка (эталон Борс ДОСТРОЕН) + единая Разведка (дизайн) + мультивыбор
tags: [handoff, s8, dinamo-bliznec, f-warm-button, adr-0022, warm-apply, bors, unified-scout, live-placement, multi-select]
updated: 2026-07-22
---

# HANDOFF 2026-07-22 · session 15

## Состояние
**S8 «Динамо-близнец»: F-warm-button ОТГРУЖЕН, живая постановка ПОДТВЕРЖДЕНА — эталон Борс ДОСТРОЕН.**
Оператор нажал «Поставить» на живом сетапе → Борс поставил реальный демо-ордер. Пункты приёмки Куратора
1/2 закрыты вживую. По слову Куратора **Разведка/Кузница РАЗБЛОКИРОВАНЫ** (были в ожидании до Борса).
Следующая большая веха — **единая Разведка** (макет+дизайн-док готовы, ждёт подписи Куратора на поле Контракта).

## Сделано в сессии
1. **Чек-лист хэндоффа session-14 пройден** (main зелёный, Борс жив, горн+самоход вживую, флот на Postgres,
   тесты 187+31, страж-дрейфа 2 дельты).
2. **Единая Разведка — ДИЗАЙН готов:** макет `_curator/design/razvedka-unified-mockup.html` (селектор
   «чьими глазами» → черновик порогов «Подтвердить для бота» → доска по ВЕРДИКТУ движка: в работе/готов·ставит/
   нужна кнопка/не берёт+причина; спарклайны) + дизайн-док `razvedka-unified-design.md`. Согласованы с Оператором.
   **ТФ = свойство генома/профиля бота (Конструктор), Разведка НАСЛЕДУЕТ** (не тумблер). Сайдбар: Конструктор
   первым в «Кузнице».
3. **F-lookahead доказан вживую:** прогон `scratchpad/market_warm_scan.py` (настоящий `warm.classify` по топ-150
   рынка) — auto-годные ЕСТЬ (CBRS/BEAT/UB/ZBT), но узкий динамический набор Борса их структурно не берёт.
4. **🔴→✅ Баг: Борса не было в селекторе Разведки** (устаревший whitelist `api.ts:173` `/Галахад|Персиваль/`,
   Борс S8 не попал) → Оператор по ошибке крутил Галахада. Фикс: whitelist + id-префикс `cd8d0534`, задеплоено
   (merge `a309239`). Куратор принял (реальный блокер операбельности).
5. **🏁 F-warm-button (ADR-0022) ПОСТРОЕН → merged → deployed → живая постановка:**
   - Подписан Куратором (вариант А, часть «добить Борса»). **0-vendor** (движковая часть уже есть: вендор
     `maybe_warm`→`_warm_one_button`; геном не тронут). Проводка: ядро `POST /scout/warm-apply` (Оператор-only,
     портал 403) + адаптер `reader.warm_apply`/`bot.py` (интент WARM_APPLY) + консоль кнопка «Поставить».
   - **Alembic 0015** (CI поймал: команда `warm_apply` не в CHECK `ck_commands_kind` — виден только на Postgres).
   - Тесты vs НАСТОЯЩИЙ вендор (reanchored ставит/OPEN·has_active·cap skip/single-shot) + адаптер + маршрут(403).
   - **Живая постановка:** Оператор → «Поставить» на 1INCH → `13:00:06 WARM-сетап ПОСТАВЛЕН (warm PENDING) +
     прогрето 1 из 1` — демо-ордер сеткой, 0 ошибок биржи. Защиты подтверждены (LDO/ONDO/CL → skip).
6. **Мультивыбор + UX-нюансы (доводки по просьбе Оператора; ЧИСТАЯ консоль, бэкенд/контракт не тронуты):**
   чек-бокс в стеке + «Поставить отмеченные · N» → ОДНА команда со списком (движок за тик разберёт пачку);
   статус сетапа: галочка (выбрал) → **⏳** песочные часы с переворотом (отправлено, ждёт 15m-тик) → **● в
   работе** (движок поставил — монета в живых ордерах); мелкий счётчик **«след. скан ~HH:MM · через Xм»** до
   ближайшей 4h-границы (UTC-сетка, закрытие 4h-свечи = плановый автоскан) у заголовка стека. Deployed.

## Решения сессии
- **F-warm-button = ADR-0022** (новая команда Контракта `warm_apply`; Оператор-only/портал не видит, Закон 5;
  0-vendor; single-shot; демо). НЕ дельта генома — манифест 2 дельты (0019/0021) неизменен.
- **Единая Разведка (дизайн):** одобрен Куратором «по духу» (правда движка / чьими глазами / ТФ от бота).
  Постройка — после Борса (теперь разблокирована).
- **ТФ — свойство бота** (`SIGNAL_TF`, `dynamic_universe.py:33` `_SIGNAL_TF="4h"` захардкожен; 1h-бот = другой
  бот); выбор ТФ → Конструктор (Ф5). Разведка показывает, не переключает.
- **Мультивыбор** — чисто консольная правка в рамках ADR-0022 (Куратору к сведению, подписи не требует).

## Код
Ветка **main**, HEAD **`c10bf7e`**. **Всё влито, дерево чистое, незакоммиченного кода НЕТ** (кроме этого
хэндоффа + строк в log/index — их коммитит следующая сессия). CI зелёный. Ветки удалены:
`fix/console-bors-razvedka-visible` (a309239), `feat/f-warm-button` (05a2972), `fix/warm-apply-migration`
(5db1339), `feat/warm-multi-select` (af47faf), `feat/warm-status-nextscan` (c10bf7e).
ADR-0022: `wiki/decisions/0022-f-warm-button-warm-apply-command.md`;
миграция `core/alembic/versions/0015_warm_apply_command.py`; progress `wiki/progress/s8-f-warm-button.md`.
**Гитигнор-скрипты** прежние (`orchestrator/rw_*`). Scratchpad `market_warm_scan.py` — переиспользуемый (вне git).

## Живой прод
- **Ядро:** свежий деплой (маршрут `/scout/warm-apply` → 401 без токена = жив+закрыт; миграция 0015 прогнана
  start.sh). healthz 200.
- **Борс `cd8d0534`:** деплой `9c4e3741` SUCCESS, свежий образ (диспатч `warm_apply` + `reader.warm_apply`),
  LIVE=1 demo, DYNAMIC/SCOUT вкл, `WARM_EACH_CYCLE=1`, на `postgres-bors`. Реальный демо-ордер 1INCH висит на
  api-demo (Оператор снимет руками).
- **Консоль:** свежий деплой (кнопка мультивыбора «Поставить отмеченные» в карточке Борса).
- **Стражи не тронуты:** Персиваль `a6df714f` (LIVE demo, деплой 2730f90c), Галахад `dd7427a5` (SCOUT, e8ffd609).

## Открытые вопросы
- **Куратору (QUEUE, ждёт отмашки):** с чего начинать разблокированную Разведку/Кузницу; **3 пункта на подпись**
  (поле «правда движка» per-coin для единой Разведки, `SIGNAL_TF` как поле профиля, пересмотр «представителя»);
  ратификация per-bot Postgres (откл. ADR-0007); F-warm-button мультивыбор — FYI.
- **Icebox №2:** UX кнопки (сейчас по всей строке стека, движок валидирует) — уточнить витриной «нужна кнопка»
  единой Разведки.

## Следующий шаг
**Дождаться отмашки Куратора** (Оператор говорит Куратору «разбор» → тот читает доклад «п.1/п.2 закрыты» и
решает порядок). Наиболее вероятно — **единая Разведка**: подписать поле Контракта «правда движка» per-coin →
строить консоль (адаптер `warm.classify` per-coin 0-vendor + перестройка `Scout.tsx` на доску-по-вердикту). Макет
и дизайн-док уже готовы (`_curator/design/razvedka-unified-*`). Строить только ПОСЛЕ подписи поля Контракта.

## Проверка для нового чата
- [ ] `main @ c10bf7e`, дерево чистое, CI зелёный (`gh run list --branch main -L 1`).
- [ ] Борс `cd8d0534` жив (`python3 orchestrator/rw_logs.py cd8d0534 60`): `warm_apply`-диспатч поднялся,
      `reconcile_on_start: {}`, нет 422, стражи не тронуты.
- [ ] Ядро: `curl -s -o /dev/null -w '%{http_code}' -X POST .../v1/instances/<uuid>/scout/warm-apply -d '{"coins":["BTCUSDT"]}'`
      → **401** (маршрут жив+закрыт). Миграция: `alembic heads` = `0015_warm_apply_command`.
- [ ] Тесты: картридж **189** + core **31** (+3 маршрута warm_apply в CI); ruff (`app tests`) чист; tsc чист;
      страж-дрейфа = **2 дельты** (вендор НЕ тронут — F-warm-button 0-vendor).
- [ ] ADR-0022 (`decisions/0022-*`) + миграция 0015 на месте; манифест генома неизменен (2 дельты).
- [ ] QUEUE с конца: F-warm-button жив (п.1/п.2 закрыты) + мультивыбор FYI + отложенные (Разведка/Кузница/3 подписи).
- [ ] Единая Разведка: макет+дизайн-док в `_curator/design/razvedka-unified-*` (вне git); решения ТФ/сайдбар — в них.
- [ ] Секреты — только Railway env (RAILWAY_*, ключи Bybit, пароли postgres-*); не в git, не эхать.
