---
type: handoff
title: HANDOFF 2026-07-21 session 13 — S8 «наведение порядка»: verified-график + кнопка скана + СВОЯ Postgres Борсу + 2 живых бага (422-журнал, scout.db-пин)
tags: [handoff, s8, dinamo-bliznec, verified-grid, scan-button, postgres, f-scout-snap, f-worker-db, bugfix]
updated: 2026-07-21
---

# HANDOFF 2026-07-21 · session 13

## Состояние
**S8 «Динамо-близнец» Веха 2 — в проде, «наведение порядка» после 3 автономных дней.** Борс
(`mfc-inst-cd8d0534`) живёт в динамике LIVE=1 (демо) на **СВОЕЙ Postgres**, вселенная собирается,
пин/гейт Вехи 2 отработали вживую многократно. Сессия шла по слову Оператора — **лимит канала
Куратора исчерпан** (не отвечает с 18.07), все решения на его ратификацию (см. QUEUE).

**Работает вживую (подтверждено логами 21.07 19:19–19:28):** свежий образ → своя Postgres →
`reconcile_on_start: {}` (сирот нет) → скаут откалибровался → `dynamic: вселенная (8 монет)` →
мягкий рестарт движка на 8/8 → **новых 422 нет**. Все три фикса сессии подтверждены на проде.

## Сделано в сессии
1. **Отчёт за 3 дня** (18→21.07): пин Вехи 2 отработал многократно (час отсрочек 240×15с при 4
   позициях → max-defer → рестарт с пином → `kept`×4, 0 сирот на мягких рестартах); вселенная
   ротируется 4h-only; working 10000→9995 (реализованный −0.05%); 731 рестарт движка за 3 дня —
   **наблюдать** (декомпозиция за окном логов; текущий ритм здоровый ~1/7.5ч).
2. **Verified-график + кнопка скана (F-scout-snap), merge `00dd3a6`:** график held-монеты =
   РЕАЛЬНАЯ сетка сделки (`scout_reader._verified_grid` = warm-реплей той же функции движка) +
   синтез снимка при уходе из находок + re-push на смену held; поле `verified` в контракте
   (schema+ядро+гвозди); консоль — бейдж «✓ сетка сделки · движок» + кнопка «⟳ Сканировать сейчас».
3. **СВОЯ Postgres Борсу** (решение Оператора «каждому боту своя, не женим»): сервис
   `postgres-bors` (postgres:16-alpine + volume + PGDATA-подкаталог) + `DATABASE_URL` → редеплой.
   **F-worker-db для Борса закрыта** — состояние переживает редеплои.
4. **2 живых бага пойманы и починены:** (а) **422-журнал** (`d80292d`) — trades/events принимают
   прошлое (journal-sync бэкфиллит биржу в свежую БД), режем только будущее; (б) **scout.db-пин**
   (`c91561c`) — `DATABASE_URL` уводил чтение адаптера в пустой Postgres воркера → вселенная
   молчала 25 мин; `ScoutReader` пинит SQLite явно.
5. Ядро редеплоено (skew-фикс, `d783021c` SUCCESS, healthz 200); консоль задеплоена (`b1bfbc52`).

## Решения сессии
- **Postgres per-bot** (не кластер/схема-на-инстанс ADR-0007): Оператор решил «каждому своя база».
  ⚠️ **ОТКЛОНЕНИЕ от ADR-0007** — на ратификацию Куратору (amendment либо «per-bot для динамик-ботов»).
- **ts-приём журналов**: trades/events — история (прошлое легально, дедуп по exec_id); equity —
  строгая (целостность серии). Уточнение семантики Контракта — на ратификацию.
- **scout.db всегда SQLite** у читателя-адаптера, даже когда воркер на Postgres (урок:
  глобальный env `DATABASE_URL` меняет ВСЕ vendor-`DB()` процесса — пинить каждого читателя).
- ADR не оформлял (лимит Куратора; решения записаны в QUEUE на его разбор).

## Код
Ветка **main**, HEAD **`398b397`**. **Всё влито, дерево чистое, незакоммиченного НЕТ.** CI зелёный
по каждому merge. Ветки сессии (влиты --no-ff): `task/s8-verified-grid`, `task/s8-journal-skew-fix`,
`task/s8-scoutdb-pin`. **Деплой-скрипты gitignored** (`.git/info/exclude`): `rw_deploy_bors.py`,
`rw_deploy_core.py`, `rw_logs.py`, `rw_check_dburl.py`, `rw_setup_bors_db.py`.

## Открытые вопросы
- **Куратору (ратификация, накопилось в QUEUE):** Postgres-per-bot vs ADR-0007; verified-поле в
  Контракте; ts-семантика журналов; F-warm-button (кнопка подхвата re-anchored PENDING — новая
  команда Контракта); F-lookahead (no-backfill + суточная калибровка — vendor-дизайн); периодический
  warm-перезапуск (способ вживления А/Б).
- **Оператору:** (1) verified-график и кнопку скана проверить глазами в консоли на живой монете
  с позицией; (2) у него есть **стратегические мысли о перестройке** динамики — обсудить с Куратором;
  (3) остальному флоту (Персиваль LIVE!, Галахад) Postgres тоже нужен — тот же класс риска, отдельным
  заходом; (4) тейк/трейлинг «сделка без тейка» — это `TRAIL_R=0.4` (после пика фикс-цель снимается,
  ведёт трейл), крутится настройкой, не баг.
- **Наблюдать:** 731 рестарт движка за 3 дня — если ритм снова участится, копать причину.

## Следующий шаг
Оператор проверяет verified-график/кнопку в консоли на живой позиции. Дальше — либо ждём Куратора
для ратификации/движковых развилок (warm-button, периодический warm), либо Оператор задаёт
стратегическую перестройку динамики. Флоту (Персиваль/Галахад) — своя Postgres тем же
`rw_setup_bors_db.py`-паттерном, когда решите (у них сейчас нет персистентности — спящий риск).

## Проверка для нового чата
- [ ] `main @ 398b397`, дерево чистое, CI зелёный (`gh run list --branch main -L 1`).
- [ ] Борс `cd8d0534` жив на СВОЕЙ Postgres: `python3 orchestrator/rw_logs.py cd8d0534 200` →
      есть `dynamic: вселенная (N монет)`, `reconcile_on_start: {}`, НЕТ `422`, НЕТ «DATABASE_URL не задан».
- [ ] `postgres-bors` сервис существует и SUCCESS (`rw_logs.py postgres-bors 5`).
- [ ] Стражи не тронуты: Персиваль `a6df714f` (LIVE боевой) / Галахад `dd7427a5` (SCOUT) — не редеплоить без нужды.
- [ ] Консоль (`console-production`, деплой `b1bfbc52`): карточка Борса → кнопка «⟳ Сканировать
      сейчас» + клик по монете → график с бейджем «✓ сетка сделки» (если есть held-позиция).
- [ ] QUEUE с конца: сводка сессии 21.07 + развилки на ратификацию Куратора (он не отвечал с 18.07).
- [ ] Тесты: картридж 186 + core 31 зелёные; ruff/tsc чисты.
- [ ] RAILWAY_* / demo-ключи / пароль postgres-bors — в Railway env (не в git, не эхать).
