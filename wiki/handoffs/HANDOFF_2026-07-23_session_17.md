---
type: handoff
title: HANDOFF 2026-07-23 session 17 — S8 per-coin бары ОТГРУЖЕНЫ+СВЕРЕНЫ (порция №1 канона) + КАНОН ОЧЕРЕДИ Куратора (5 порций) + 4 консольные доводки + диагноз cap=16
tags: [handoff, s8, per-coin-bars, kanon-ocheredi, progrev-held, warm-fate, greetsya, concurrency-cap, console]
updated: 2026-07-23
---

# HANDOFF 2026-07-23 · session 17

## Состояние
**S8→Ф5. Порция №1 канона (per-coin бары) ЗАКРЫТА вживую; впереди порция №2 (прогрев held).**
Куратор утвердил **КАНОН ОЧЕРЕДИ из 5 порций** (одна за раз). Per-coin volnorm-бары построены→
merged→задеплоены на Борса→живая сверка прошла (в карточке разные бары: 1INCH 1.75/3, WLFI 2.5/4.25,
ни одной плоской 2.0/3.5). Плюс 4 консольные доводки по жалобам Оператора. main @ **2dbe7b9**,
дерево чистое, CI зелёный, незакоммиченного НЕТ.

## КАНОН ОЧЕРЕДИ (Куратор, утверждён Оператором; дубль в `citadel/S6-porcii-inzheneru.md`)
1. **Живая сверка per-coin баров** — ✅ ЗАКРЫТА этой сессией.
2. **Прогрев held (~полдня)** — ← СЛЕДУЮЩАЯ. План готов (`progress/s8-progrev-held.md`), подход подписан.
3. **Этап 1 «Сигнальный журнал»** = запуск переката. План у Куратора: `citadel/etap1-signal-journal-plan.md`.
4. **Конструктор** (пока журнал зреет): политика входа / вселенная live-эндпоинт (бары п.1 = фундамент) / скринер; OOS-паспорт отдельно.
5. **Этап 2 «Теневой клиент»** — гейт: полнота журнала доказана.

## Сделано в сессии (марафон)
1. **Чек-лист session-16 пройден** (main зелёный, Борс engine-источник жив, тесты 217+32, страж-дрейфа 2 дельты, VVV закрыт).
2. **🏁 Порция №1 — per-coin бары построены→merged→деплой→СВЕРКА.** Вендор УЖЕ считает volnorm-бары
   (`scout/bars.py`, K1/K2 глобальные) и кладёт mb1/mb2/bar_source в `scout_list` — `scan_list_rows` их
   дропал. Проводка 0-vendor (scout_reader несёт бары + placeable_scan per-coin mb, убран форс
   `_DEFAULT_COIN`; dynamic_universe `coin_block`+страж положительности, STICKY held-заморозка,
   `_maybe_write` сигнатура по содержимому, `view()` бары; config `DYNAMIC_BARS_REFRESH_S`). Консоль:
   колонка «Бары». Тесты vs вендор +10 (227). merge **`2c01f8e`**. Куратор: окно принял задним числом,
   провал вселенной при редеплое = известный хвост (гейт до реальных денег). Деплой Борса `--go --live
   --engine` (стражи целы). Живая сверка: differentiated бары в карточке.
3. **4 консольные доводки (жалобы Оператора, чистая консоль, мой домен):**
   - warm-пометка (⏳/судьба) переживает навигацию: карточка `b9bcba1` + Разведка унифицирована общим
     ключом `mfc.warmSentAt.<бот>` `2a32dde`.
   - адаптивная точность цены на графике сетапа (дешёвые монеты не сливаются) `f700cf0`.
   - судьба warm — сквозняк с Разведкой (брать 4h-снимок, не 1h) + метка «греется» `67b47ee`.
   - «греется без чек-бокса» `f988b8f` → **ОТКАЧЕН `e64de7c`** (хрупко: мигание вердикта прятало кнопку +
     переобещание при полном cap). Итог: чек-бокс у неотмеченной всегда, «греется» только после пометки.
4. **Диагноз «годные не встают в ордера» (WLFI/STABLE):** НЕ баг. Лог Борса дословно
   `WLFIUSDT: cap=16 — warm-сетап не влезает → пропуск`. `CONCURRENCY_CAP=16` полон (14 ног + 3 = 17 > 16),
   штатная защита экспозиции; освободятся — движок возьмёт сам.

## Решения сессии
- **Окно engine-источника принято задним числом** (2 границы + 6ч + добро Оператора; откат = ещё редеплой, вреднее).
- **Per-coin бары** — подпись Куратора (метод volnorm честный, 3 уточнения впаяны). ADR не нужен (в рамках разъёма).
- **«Греется без чек-бокса» откачен** — прятать кнопку по мигающему auto-вердикту хрупко; консоль display-only, cap — движок.
- **CONCURRENCY_CAP выше 16 — НЕ автономно.** Геном-потолок `hi:16` (config/knobs.py), паттерн MAX_LEVERAGE hi:5 («вне честных OOS → ADR+ре-бэктест»). Развилка Куратору (его гейт: риск/геном/деньги).

## Код
Ветка **main**, HEAD **`2dbe7b9`**. **Весь КОД влит, дерево чистое, незакоммиченного НЕТ.** CI зелёный.
Ключевые SHA: per-coin бары merge `2c01f8e`; консольные `b9bcba1`/`2a32dde`/`f700cf0`/`67b47ee`/`e64de7c`
(последняя — откат). Картридж-файлы: `bots/pifagor-cartridge/app/{scout_reader,dynamic_universe,config}.py`.
Консоль: `console/src/{screens/fleet/BotCard.tsx,screens/Scout.tsx,screens/scout/ScoutDetail.tsx,lib/scout.ts}`.
Планы: `progress/{s8-per-coin-bars,s8-progrev-held}.md`. Скрипты гитигнор `orchestrator/rw_*`.

## Живой прод
- **Ядро:** `core-production-429b.up.railway.app` (healthz 200; warm-apply без токена 401).
- **Борс `cd8d0534`:** engine-источник + **per-coin бары LIVE** (`placeable-скан`, `dynamic: вселенная`,
  `knobs(eff) cap=16`, 0×422). env `DYNAMIC_SOURCE=engine`/`LIVE=1`/`DYNAMIC=1`/`SCOUT=1`. Стражи
  Персиваль `2730f90c`/Галахад `e8ffd609` неизменны.
- **Консоль:** `console-production-f533.up.railway.app` (бандл `index-K6UIExK9.js`): колонка «Бары»,
  warm-пометка переживает навигацию, точность цены графика, «греется» после пометки.

## Открытые вопросы
- **Куратору (QUEUE, ждёт «разбор» Оператора):** (a) **CONCURRENCY_CAP > 16?** — риск/геном-развилка
  (санкц-дельта + ре-бэктест + рост экспозиции); (b) честный показ «нет места (cap)» вместо «греется» =
  добавка cap-остатка в engine-телеметрию (правка Контракта) — отдельным шагом.
- **Икебокс/хвосты:** held-монета без свежего placeable-скана кажет бары `—` (фолбэк дефолт, ок);
  пост-редеплой провал вселенной ~18мин (эфемерная scout.db) — чинит порция №2 (прогрев held); полный
  персист scout.db/coins.json — техдолг-гейт до реальных денег.

## Следующий шаг
**Порция №2 «прогрев held»** по плану `progress/s8-progrev-held.md` (подход подписан в каноне): правка
`dynamic_universe.tick` — на старте (coins.json нет + held известны из build_monitor) провайдер СРАЗУ
пишет вселенную из held, до скаута (окно неведения позиций 20мин→~тик). Одноразово (пока `_written_sig`
пуст), source-агностично, 0-vendor. → тесты vs НАСТОЯЩИЙ вендор → merge → деплой Борса → сверка
(редеплой → held в coins.json с 1-й минуты). ЛИБО сперва дождаться разбора Куратора по cap-развилке.

## Проверка для нового чата
- [ ] `main @ 2dbe7b9`, CI зелёный (`gh run list --branch main -L 1`), дерево чистое.
- [ ] Борс `cd8d0534` жив (`python3 orchestrator/rw_logs.py cd8d0534 300`): `placeable-скан`,
      `dynamic: вселенная`, `knobs(eff) cap=16`, 0×422; стражи Персиваль/Галахад штампы неизменны.
- [ ] Ядро: `curl -s -o /dev/null -w '%{http_code}' https://core-production-429b.up.railway.app/healthz` → **200**; warm-apply без токена → 401.
- [ ] Тесты: картридж **227** + core **32** (локально); ruff (`app tests`) чист; tsc консоли чист; **страж-дрейфа = 2 дельты**.
- [ ] Консоль (`console-production-f533.up.railway.app`, Cmd+Shift+R): колонка «Бары» per-coin; warm-пометка
      переживает навигацию (карточка↔Разведка); «греется» только ПОСЛЕ пометки, у неотмеченной — чек-бокс.
- [ ] QUEUE хвост: КАНОН 5 порций; порция №1 закрыта; **развилка CONCURRENCY_CAP** + порция №2 ждут.
- [ ] Секреты — только Railway env (RAILWAY_*, ключи Bybit demo, пароли postgres-*); не в git/лог/чат.
