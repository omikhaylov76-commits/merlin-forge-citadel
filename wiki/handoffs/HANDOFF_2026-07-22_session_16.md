---
type: handoff
title: HANDOFF 2026-07-22 session 16 — S8 Единая Разведка + engine-источник (F-lookahead ВЫЛЕЧЕН) + 3 консольные доводки; бары per-coin у Куратора
tags: [handoff, s8, dinamo-bliznec, f5, razvedka-unified, engine-source, f-lookahead, dynamic-universe, warm-fate, scan-indicator, bars, volnorm]
updated: 2026-07-22
---

# HANDOFF 2026-07-22 · session 16

## Состояние
**S8→Ф5: три крупные отгрузки, все живут вживую.** (1) **Единая Разведка** (правда движка per-coin,
доска-по-вердикту) — merged+deployed, принята Куратором. (2) **Engine-источник** (F-lookahead
ВЫЛЕЧЕН: вселенная движка от `warm.classify`, не от скаут-курации) — merged+deployed, подтверждён
живьём (стек 7 placeable, ENA/KAITO вернулись). (3) **3 консольные доводки** (судьба warm-сетапа +
устойчивый скан-индикатор) — deployed. **Открыто одно:** бары per-coin (проводка volnorm) ждут
кивка Куратора. main @ **c6a05d1**, дерево чистое, CI зелёный, незакоммиченного НЕТ.

## Сделано в сессии (марафон)
1. **Чек-лист session-15 пройден** (main зелёный, Борс жив, тесты 204+31, страж-дрейфа 2 дельты).
2. **🏁 Единая Разведка построена→merged→deployed** (подпись Куратора «Строй», 3 пункта): поле
   `engine` telemetry-scout (правда движка per-coin: kind/auto_eligible/reanchored/in_universe+сетка;
   extra=forbid, kind required-nullable) через `scout_reader._truth`→`warm.classify` по вселенной;
   консоль доска-по-ВЕРДИКТУ (в работе/движок ставит/нужна кнопка/не берёт+причина), «представитель»
   убран (селектор «чьими глазами»), ТФ=readout-гвоздь «4h от бота», сайдбар Конструктор-первым.
   0-vendor. Куратор **принял целиком** (сверил в коде). merge `c4e199a`.
3. **🔴→✅ RED-фикс живого бага:** доска Разведки висла после смены порогов дозора (единственный
   scout-POST за всё время). Корень — триггер `_push_scout` слал только на новый scan_ms; смена
   порогов пересобирает находки в ТОЙ ЖЕ 4h-границе → пуш не уходил. Фикс `44a9df2`: `_scout_sig` —
   пуш на смену СОДЕРЖИМОГО. + консоль авто-обновление доски ~25с `7d943d9`.
4. **🔍 Диагноз F-lookahead (тревога Оператора «а есть чем торговать»):** прогон настоящего
   `warm.classify`+`scan.classify` по топ-200 рынка — **13 placeable, Борс брал 2**. Косяк =
   **КРИТЕРИЙ ОТБОРА вселенной: скаут-скор ≠ движок-placeable** (7/10 слотов стека = None-монеты;
   6 годных не в scan_list). Дочищено по логам (`dynamic: вселенная` стек 10, 3 торгуемых).
5. **🏁 Engine-источник (вариант 3, подпись Куратора после разворота с в1):** провайдер в
   engine-режиме отбирает placeable (`warm.classify` PENDING auto|reanchored) из курированного пула
   scout_list. `DYNAMIC_SOURCE` env, дефолт `scout`=байт-в-байт (fail-closed), Борсу `engine`.
   Адверс-фикс: `placeable_scan` ФОРСИТ `_DEFAULT_COIN` (движок REPLACE-ит coins.json — не наследовать
   статик-mb AAVE 2.5/4.0). Efficiency-фикс `99edcf9`: гейт дорогого 155-classify дешёвым курсором.
   merge `18248b7`. **Живая сверка:** `placeable-скан 155 → годных 7`, `вселенная: 1INCH,AVAAI,BANK,
   ENA,KAITO,VANA,ZAMA` — ВСЕ placeable, ENA/KAITO (структурно терявшиеся) вернулись.
6. **Бары per-coin — обсуждение методологии, развилка СДУЛАСЬ:** Оператор хотел per-coin бары «по
   всей истории»; я поднял look-ahead-риск (авторы движка сами отвергли профит-свип); Оператор
   доразмыслил → «recent-window + периодический пересчёт» = **ровно volnorm** (уже per-coin). ⇒ риск
   снят, осталась ПРОВОДКА (feed volnorm/config баров из scout_list в coins.json вместо плоского
   2.0/3.5). **Ждёт кивка Куратора** + выбор частоты обновления.
7. **3 консольные доводки Оператора** (чистая консоль, 0-контракт, merge `c07340e`): #2 судьба
   warm-сетапа в карточке бота (в работе/⏳/✗ не взято+ПРИЧИНА из вердикта scout-снимка; WarmCell);
   доска Разведки судьбу уже кажет колонками; #3 устойчивый скан-индикатор (localStorage, переживает
   уход/возврат, «идёт на боте ~1-2 мин, можно уходить»).

## Решения сессии
- **Единая Разведка:** поле `engine` telemetry-scout аддитивно; Куратор подписал 3 пункта
  (правда движка per-coin · SIGNAL_TF=readout-гвоздь, knob→Кузница · представитель убран). В рамках
  Контракта v1 (ADR-0016), новых команд/миграций нет.
- **F-lookahead → вариант 3 (engine-источник):** Куратор подписал подход (разворот с в1 после моей
  дочистки — критерий, не частота). 0-vendor, разъём ADR-0019 переиспользован (нового ADR НЕ
  требовал — «не плоди второй»), страж-дрейфа = 2 дельты. Возраст-фильтр НЕ трогаем (решение
  Оператора: молодые монеты пока мимо).
- **Force `_DEFAULT_COIN` в placeable_scan** (адверс-ревью): движок при dynamic REPLACE-ит coins.json
  на `_DEFAULT_COIN`, адаптер обязан классифицировать теми же 2.0/3.5 (тест-гвоздь есть).
- **Бары:** методология-риск (переобучение) снят — volnorm = recent-window per-coin, не подгон.
  Осталась проводка; ADR не нужен (в рамках разъёма). Ждёт формальной подписи Куратора (меняет
  пороги входа Борса).

## Код
Ветка **main**, HEAD **`c6a05d1`**. **Весь КОД влит, дерево чистое** (незакоммиченного кода НЕТ; кроме
ЭТОГО хэндоффа + строк в log/index — штатно, их коммитит следующая сессия). CI зелёный.
Ветки удалены: `feat/razvedka-unified` (→c4e199a), `fix/*`/`feat/*` RED-фикса и авто-обновления
(44a9df2/7d943d9), `feat/dynamic-engine-source` (→18248b7), `fix/engine-scan-gate` (→99edcf9),
`feat/warm-fate-scan-ux` (→c07340e).
Ключевые файлы: `bots/pifagor-cartridge/app/{scout_reader,dynamic_universe,mapper,config,bot}.py`
(единая Разведка + engine-источник), `core/app/routes_telemetry.py` (ScoutEngineIn),
`contracts/telemetry-scout.schema.json` (поле engine), `console/src/{lib/scout.ts,screens/Scout.tsx,
screens/scout/*,screens/fleet/BotCard.tsx,lib/nav.ts}`. Планы: `wiki/progress/{s8-razvedka-unified,
s8-dynamic-engine-source}.md`. Гитигнор-скрипты `orchestrator/rw_*` (rw_deploy_bors.py +`--engine`).
Scratchpad `market_warm_scan.py`/`flookahead_trace.py`/`probe_*` — переиспользуемые (вне git).

## Живой прод
- **Ядро:** свежий деплой (healthz 200; warm-apply без токена 401 = жив+закрыт).
- **Борс `cd8d0534`:** engine-источник LIVE — env `DYNAMIC_SOURCE=engine`, `LIVE=1`/`DYNAMIC=1`/
  `SCOUT=1`/самоход `WARM_EACH_CYCLE=1`. `placeable-скан 155→N годных за ~0.16с`, `reconcile {}`,
  0×422. Стражи Персиваль `a6df714f`/Галахад `dd7427a5` штампы неизменны.
- **Консоль:** свежий деплой (3 доводки: судьба warm, устойчивый скан-индикатор).

## Открытые вопросы
- **Куратору (QUEUE, ждёт кивка):** бары per-coin — проводка volnorm/config баров из scout_list в
  coins.json (методология-риск снят) + выбор частоты обновления (ежедневно Этап A vs реже/стабильнее).
  Оператор говорит Куратору «разбор».
- **Оператору:** визуальная сверка 3 доводок (Cmd+Shift+R): судьба warm в карточке, «не взято+причина»,
  устойчивый скан-индикатор.
- **Icebox/хвосты:** (1) телеметрия `_truth` (Разведка engine-поле) для динамик-бота наследует
  статик-mb — пре-существующий, чинится тем же per-coin барами; (2) бот-карта warm-причина недоступна
  для монет вне scout-снимков (показывает «не взято» без причины); (3) состав пула — почему часть
  рыночных годных вне scan_list (скор-кап/окно), не критерий.

## Следующий шаг
**Оператор → Куратору «разбор» по барам** → кивок → построить **проводку volnorm-баров** (per-coin
mb движку вместо плоских 2.0/3.5): `scan_list_rows` +mb1/mb2; `placeable_scan` классифицирует per-coin
mb (убирает force `_DEFAULT_COIN`); стек-item хранит mb; `_maybe_write` пишет per-coin в coins.json
(held-пин → фолбэк); тесты vs НАСТОЯЩИЙ вендор + регресс scout. ЛИБО (по слову Оператора) обкатать
engine-источник день (как стек ловит свежие placeable, оживают ли средние колонки, ставит ли самоход).

## Проверка для нового чата
- [ ] `main @ c6a05d1`, CI зелёный (`gh run list --branch main -L 1`); дерево чистое от КОДА
      (в статусе только этот хэндофф + log/index — их коммитит эта сессия первым делом).
- [ ] Борс `cd8d0534` engine-источник жив (`python3 orchestrator/rw_logs.py cd8d0534 300`):
      `placeable-скан пула 155 → годных N`, `dynamic: вселенная (...)`, `reconcile {}`, 0×422,
      env `DYNAMIC_SOURCE=engine`; стражи Персиваль/Галахад штампы неизменны.
- [ ] Ядро: `curl -s -o /dev/null -w '%{http_code}' https://core-production-429b.up.railway.app/healthz`
      → **200**; warm-apply без токена → **401**.
- [ ] Тесты: картридж **217** + core **32** (локально; +CI Postgres); ruff (`app tests`) чист; tsc
      консоли чист; **страж-дрейфа = 2 дельты** (вендор diff пуст — engine-источник 0-vendor).
- [ ] Консоль: 3 доводки живы — судьба warm в карточке бота (в работе/⏳/не взято+причина),
      устойчивый скан-индикатор «можно уходить».
- [ ] QUEUE хвост: engine-источник живая сверка ✅ + бары-развилка сдулась до проводки volnorm
      (ждёт кивка Куратора + частота обновления).
- [ ] Секреты — только Railway env (RAILWAY_*, ключи Bybit demo, пароли postgres-*); не в git/лог/чат.
