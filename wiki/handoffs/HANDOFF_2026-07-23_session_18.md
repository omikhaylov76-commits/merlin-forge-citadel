---
type: handoff
title: HANDOFF 2026-07-23 session 18 — ADR-0023 cap→24 ОТГРУЖЕН + порция №2 «прогрев held» ОТГРУЖЕНА (план исправлен разведкой) — обе живы
tags: [handoff, s8, concurrency-cap, adr-0023, progrev-held, prewarm, boot, dinamo-bliznec, console]
updated: 2026-07-23
---

# HANDOFF 2026-07-23 · session 18

## Состояние
**S8→Ф5. Канон очереди: порции №1 (бары ✅) и №2 (прогрев held ✅ — ЭТА сессия) закрыты вживую.**
Отгружены и подтверждены на облаке ДВА шага: **ADR-0023 CONCURRENCY_CAP 16→24** (динамик Борс) и
**порция №2 «прогрев held»** (движок ведёт позиции с 1-й минуты после редеплоя). main @ **8d41d5f**,
дерево чистое, CI зелёный, незакоммиченного НЕТ. 🔒 демо, стражи Персиваль/Галахад не тронуты.

## КАНОН ОЧЕРЕДИ (Куратор, 5 порций — по одной)
1. Живая сверка per-coin баров — ✅ (session 17).
2. **Прогрев held — ✅ ЗАКРЫТА этой сессией** (v2 boot-шаг).
3. **Этап 1 «Сигнальный журнал»** ← СЛЕДУЮЩАЯ. План у Куратора: `citadel/etap1-signal-journal-plan.md`.
4. Конструктор (пока журнал зреет).
5. Этап 2 «Теневой клиент» — гейт: полнота журнала.

## Сделано в сессии
1. **Чек-лист session-17 пройден** (main зелёный, Борс cap=16 жив, тесты 227+32, страж-дрейфа 2 дельты, auth-гейт цел).
2. **🏁 ADR-0023 cap 16→24** — Куратор подписал развилку ПОСЛЕ хэндоффа S17 (сперва «не сейчас», затем
   пересмотр по слову Оператора). Правка узкая: геном `knobs.py` единственный потолок `hi:16→24`, движок
   не тронут. Страж-дрейфа 2→3 дельты, тест vs вендор (validate 24→ok/25→fail, дефолт=8). merge `00942f1`,
   деплой Борса env `CONCURRENCY_CAP=24`, живая сверка `knobs(eff) cap=24`. Дефолт эталона/флот/стражи не сдвинуты.
3. **Диагноз «нет сетапов» (жалоба Оператора):** НЕ баг. Борс редеплоился недавно (окно прогрева scout.db) +
   таймзона (логи UTC vs карточка EEST/UTC+3). «кап 10» на карточке = размер вселенной, НЕ concurrency.
4. **🏁 Порция №2 «прогрев held» — план ИСПРАВЛЕН разведкой ДО кода.** v1 «правка tick» НЕ работал (движок
   читает coins.json на старте, стартует раньше адаптера, gen=0 не рестартит, defer при позициях). v2:
   **boot-шаг `python -m app.prewarm_held` в `start.sh` ДО `engine_supervise`** — held через
   `PifagorReader(owner=False)` из персист-БД → пишет coins.json (дефолт-бары, пол-на-пустоту). `_write_atomic`
   → общий `_write_coins_atomic` (байт-в-байт). Best-effort. 0-vendor (страж=3). merge `66d23ca`, деплой,
   **сверка 11:39: `прогрев held ДО движка (5 монет)` → движок `стек исполнения 5/5` (не дефолт-16) →
   reconcile 5/5 kept.** Окно неведения позиций закрыто.

## Решения сессии
- **ADR-0023 «потолок 24 динамик-характера»** = 3-я санкц-дельта генома (реестр ADR-0019 обновлён до трёх).
  Только Борс (env=24); эталон=8/флот=16 не тронуты; честная пометка статистики + ре-бэктест = техдолг.
- **Порция №2 — v2 boot-шаг вместо tick** (отклонение от подписанного плана v1). Причина в разведке (движок
  boots первым, tick-прогрев не подхватывается). Сделал сам по автономии (0-vendor/демо) → **флаг Куратору в
  QUEUE на подпись подхода**. ADR не заводил (в рамках существующего разъёма ADR-0019).
- **Диагноз cap-остатка**: показ «нет места» + витрина-потолок консоли — отложены Куратору (ниже).

## Код
Ветка **main**, HEAD **`8d41d5f`**. **Весь КОД влит, дерево чистое, незакоммиченного НЕТ.** CI зелёный.
Ключевые SHA: cap merge `00942f1` (feat `e6a1dd4`); порция №2 merge `66d23ca` (feat `2c03a31`).
Файлы cap: `bots/pifagor/vendor/config/knobs.py`, `bots/pifagor-cartridge/tests/{genome_manifest.json,
test_genome_drift_guard.py,test_knobs_cap.py}`, `wiki/decisions/0023-*.md`.
Файлы порции №2: `bots/pifagor-cartridge/app/{dynamic_universe.py,prewarm_held.py}`, `start.sh`,
`tests/test_prewarm_held.py`. Скрипт gitignore `orchestrator/rw_deploy_bors.py` (+`CONCURRENCY_CAP=24` в bors_upsert).

## Живой прод
- **Ядро:** `core-production-429b.up.railway.app` (healthz 200; warm-apply реальный путь
  `/v1/instances/{id}/scout/warm-apply` без токена → 401; `/docs` 404; `/v1/*` без токена 401).
- **Борс `cd8d0534`:** `cap=24` + **прогрев held на бооте** (`прогрев held ДО движка` → стек held, не
  дефолт-16). env `DYNAMIC_SOURCE=engine`/`LIVE=1`/`DYNAMIC=1`/`SCOUT=1`/`CONCURRENCY_CAP=24`/`BYBIT_DEMO=1`.
  Стражи Персиваль `2730f90c`/Галахад `e8ffd609` неизменны. 0×422.
- **Консоль:** `console-production-f533.up.railway.app` (бандл `index-K6UIExK9.js` — эту сессию НЕ трогали).

## Открытые вопросы
- **Куратору (QUEUE, ждут «разбор»):** (a) **подпись подхода порции №2 v2** (boot-шаг вместо tick —
  отклонение от плана); (b) **консольный слайдер `console/src/lib/knobs.ts:28` `max:16`** — витрина-потолок
  Конструктора отстал от генома (24), глобальный (все профили) — витрина-развилка; (c) **cap-остаток в
  engine-телеметрию** (одобрено — микро-порция, «нет места · ноги N/24» вместо «греется»).
- **Оператору (к сведению):** карточка Борса «стек пуст» между 4h-сканами — норма (не регресс). Оживёт на
  скане (плановый на 4h-границе) / наливе ордера / ручном warm. cap=24 и прогрев — backend, на карточке не видны.
- **Икебокс/техдолг:** полный персист scout.db/coins.json (окно неведения для бота БЕЗ позиций) — гейт до
  реальных денег; ре-бэктест cap=24 (честная пометка статистики Борса).

## Следующий шаг
**Порция №3 «Этап 1 — Сигнальный журнал»** по канону. Нужен **план Куратора**
(`citadel/etap1-signal-journal-plan.md`, его файл вне репо) + желательно его «разбор» по порции №2 (подпись
подхода) и трём пунктам QUEUE. ЛИБО — если Оператор попросит «разбор» у Куратора — дождаться его ответов,
затем брать №3. Норма «≤1 поведенческое изменение в день»: сегодня Борс получил ТРИ (бары+cap+прогрев) —
следующее изменение поведения лучше следующим днём.

## Проверка для нового чата
- [ ] `main @ 8d41d5f`, CI зелёный (`gh run list --branch main -L 1`), дерево чистое.
- [ ] Борс `cd8d0534` жив (`python3 orchestrator/rw_logs.py cd8d0534 300` — boot-регион): `прогрев held ДО
      движка` (если были held), `knobs(eff) … cap=24`, `стек исполнения` на held (не 16), 0×422; стражи
      Персиваль `2730f90c`/Галахад `e8ffd609` штампы неизменны.
- [ ] Ядро: `curl -s -o /dev/null -w '%{http_code}' https://core-production-429b.up.railway.app/healthz` → **200**.
- [ ] Тесты: картридж **239** + core **32** (локально); ruff (`app tests`) чист; tsc консоли чист;
      **страж-дрейфа = 3 дельты** (ADR-0019 монеты + ADR-0021 warm-ритм + ADR-0023 cap).
- [ ] QUEUE хвост: 3 пункта Куратору (порция №2 v2 подпись · console `knobs.ts:16` · cap-остаток телеметрия);
      канон — порция №3 «Сигнальный журнал» следующая.
- [ ] Секреты — только Railway env (RAILWAY_*, ключи Bybit demo, пароли postgres-*); не в git/лог/чат.
- [ ] Время: логи Railway/движок в UTC, Оператор в EEST (UTC+3) — при докладе конвертируй.
