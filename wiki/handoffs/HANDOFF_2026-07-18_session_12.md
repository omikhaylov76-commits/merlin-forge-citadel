---
type: handoff
title: HANDOFF 2026-07-18 session 12 — S8 «Динамо-близнец» ВЕХА 1 ЗАКРЫТА ВЖИВУЮ (Борс в динамике, dry-run); Слой 3+аудит+триппвайр в main; план Вехи 2 на подписи
tags: [handoff, s8, dinamo-bliznec, dynamic-universe, veha-1-done, veha-2-plan, adr-0020, pin, bors, dry-run]
updated: 2026-07-18
---

# HANDOFF 2026-07-18 · session 12

## Состояние
**🏁 S8 «Динамо-близнец» ВЕХА 1 ЗАКРЫТА ВЖИВУЮ.** Борс (сервис `mfc-inst-cd8d0534`) работает в динамике
на облаке (dry-run): свой скаут сканит живой Bybit → находит сетапы → провайдер собирает рабочую вселенную
→ супервизор рестартит ТОЛЬКО движок → движок торгует (считает) по динамическим монетам вместо дефолтных 16.
Все 5 проверок пройдены (см. log 2026-07-18). Слои 1–3 + триппвайр в main, CI зелёный. **Веха 2 (торговля
из динамики через ПИН) — план готов, ждёт подписи Куратора + гейт Оператор+Куратор на `LIVE_TRADING=1`.**

Планы: Веха 1 — `wiki/progress/s8-dinamo-bliznec-v1.md` (Слои 1–4 ✅). Веха 2 — `wiki/progress/s8-dinamo-bliznec-v2.md`.

## Сделано в сессии
1. **Слой 3 (канал критериев + консоль), merge `42757eb`:** ядро `dynamic_settings` (миграция **0014** +
   `routes_dynamic`: PUT operator+audit / GET `/self`; **ADR-0020**) + картридж (`dynamic_overrides` re-fetch
   живьём D1 + фильтры скор/свежесть) + консоль (раздел 5 «Динамика» + `BotCard` секция «Стек»).
2. **Аудит 5 линз (архитектура·швы·баги·флот·законы) → 🔴 SHOWSTOPPER пойман ДО прода:** `findings_for_universe`
   читал `f.get("state")`, а вендор `build_scout` кладёт стадию под `status` → стек не наполнялся бы никогда
   (мок маскировал — урок [[vendor-integration-tests-not-mocks]]). Фикс + **вендор-интеграционный тест**
   (реальная storage.db→build_scout→провайдер). Merge `e6d31f3`.
3. **Триппвайр гейта Вехи 2 (условие Куратора), merge `f2002ca`:** `DYNAMIC+LIVE_TRADING=1` (пин не готов) →
   провайдер fail-closed НЕ активируется + тест.
4. **Слой 4 — деплой Борса в динамику ВЖИВУЮ:** узкие `orchestrator/rw_deploy_bors.py` + `rw_deploy_core.py`
   (gitignored, Bash-allow Оператора, стражи флота внутри). Ядро (0014) + консоль (railway up) обновлены.
5. **План Вехи 2 (пин) Куратору** (QUEUE + progress-v2, commit `0eb72c9`).

## Решения сессии
- **ADR-0020** (`wiki/decisions/0020-dynamic-settings-from-core.md`): канал критериев Динамики, механизм
  **D1** (провайдер foreground читает JSON-файл ЖИВЬЁМ + периодический re-fetch, БЕЗ рестарта — расходится
  с дозором осознанно) + «Ремонт после аудита».
- **ADR-0019** гейт Вехи 2 → **двухчастный** (не рестартить при позициях И пин символа) — досмотр Куратора.
- **Триппвайр** — вынесен в код ДО Слоя 4 (фэт-фингер `LIVE=1` при деплое Борса).
- **План А (выбор Оператора):** Борс → динамика на ТЕКУЩЕМ (общем GAWAIN) ключе, РОВНО 3 флага; починку
  общих ключей флота — отдельным шагом (money-риск go-live, в техдолге).
- **Опознание Борса** — по подтверждённому Оператором `MF_INSTANCE_ID=cd8d0534` (по BYBIT_API_KEY сломано:
  4 бота флота на одном demo-ключе — известное состояние log 78, ключи в `.env` не биндились на картриджи).

## Код
Ветка **main**, HEAD **`0eb72c9`**. **Весь КОД в main, CI зелёный.** Коммиты сессии: `7ed665c`/`befe…`нет —
Слой3 `42757eb`(merge)·`6122e3a`(fix E501+вики); аудит `d2121be`/`e6d31f3`(merge); триппвайр `f2002ca`(merge);
доки `5d54aa2`/`32a52ea`(Веха1)/`0eb72c9`(план V2). **НЕТ `[~]` с незакоммиченным кодом.** Незакоммичены только
хэндофф-доки (этот файл + `log.md` + `index.md`, per /handoff) — закоммитить первым делом в новом чате.
**Деплой-скрипты** `orchestrator/rw_deploy_{bors,core}.py` — **gitignored** (в `.git/info/exclude`), стабильные
пути для Bash-allow; НЕ в git (как прецеденты).

## Открытые вопросы
- **Куратору (Веха 2):** подпись развилок **F-restart** (механизм гейта рестарта в shell-супервизоре: флаг-файл
  `positions_open` vs пин-достаточен; реком A+пин), **F-pin-scope** («полный финал» слота), **F-key** (чинить
  общий ключ Борса в том же LIVE-деплое или отдельно). См. QUEUE + progress-v2.
- **Оператору (Веха 2):** ⛔ явный гейт на `LIVE_TRADING_ENABLED=1` (демо-ордера) — вместе с Куратором.
- **Техдолг (по пути):** общие ключи флота (Борс/Ланселот/Гавейн на GAWAIN); супервизор `TERM→KILL`; sync-гвоздь `DYNAMIC_DEFAULTS`.

## Следующий шаг
**Веха 2 (пин + торговля из динамики), по плану `wiki/progress/s8-dinamo-bliznec-v2.md`:** с подписи Куратора
(F-restart/F-pin-scope) → код: (1) `bot.py` передаёт провайдеру `held` (символы с живой позицией/ордером из
build_monitor/orders_open_all); (2) провайдер **пришпиливает** held (не выкидывает, coins.json всегда их несёт);
(3) `start.sh engine_supervise` гейт «не рестартить при позициях» (заменить no-op `:47`); (4) снять триппвайр;
(5) тесты vs настоящий вендор → ветка, code-review, merge. Затем ⛔ гейт Оператор+Куратор → деплой Борса
`LIVE_TRADING=1` (демо) → живая сверка (демо-ордер по динамике, пин держит позицию). 🔒 Реальные ключи — НЕ здесь.

## Проверка для нового чата
- [ ] `main @ 0eb72c9`, дерево чистое, CI зелёный (`gh run list --branch main -L 1`); сначала закоммить
      хэндофф-доки (этот файл + log + index — незакоммичены).
- [ ] Веха 1 жива на облаке: Борс `mfc-inst-cd8d0534` в динамике (dry-run). **НЕ трогать без Вехи 2.** Стражи
      флота: Персиваль `a6df714f` (LIVE=1 боевой) / Галахад `dd7427a5` (SCOUT=1) — не редеплоить.
- [ ] `ADR-0020` (`decisions/0020-*`) + `ADR-0019` (гейт Вехи 2 двухчастный) — прочитать.
- [ ] Планы: `progress/s8-dinamo-bliznec-v1.md` (Веха 1 ✅) + `-v2.md` (Веха 2, ждёт подписи).
- [ ] `_curator/QUEUE.md` с конца: Веха 1 закрыта + план Вехи 2 (F-restart/F-pin-scope/F-key) на подпись.
- [ ] Триппвайр в силе: `DYNAMIC+LIVE=1` без пина → провайдер fail-closed (тест `test_live_trading_tripwire`).
- [ ] Деплой-скрипты `orchestrator/rw_deploy_{bors,core}.py` (gitignored) — опознание Борса по cd8d0534, стражи внутри.
- [ ] Облако: ядро `core-production-429b` (свежий :main + 0014, `/v1/dynamic/settings/self` = 401 не 404),
      консоль `console-production-f533` (Слой 3). RAILWAY_* / demo-ключи — в `orchestrator/.env` (gitignored, НЕ трогать/эхать).
- [ ] Техдолг Вехи 2 в `_curator/tech-debt.md` (раздел S8): пин-гейт, общие ключи, TERM→KILL, DEFAULTS-гвоздь.
