# Схемы телеметрии (заглушка)
JSON-схемы полей появятся в contracts/*.schema.json на Ф1 вместе с paper-bot —
schema-first: сначала фиксируем схему, потом код по ней. Правки схем = новая версия контракта.
