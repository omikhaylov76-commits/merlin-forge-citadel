# -*- coding: utf-8 -*-
"""scout — сервис «Сканер-разведчик» (Веха 7, advisory scout).

Отдельный ЛЁГКИЙ keyless-сервис (тот же репозиторий, свой entrypoint `scout/main.py`):
сканирует широкую вселенную биржи на спелые сетапы `detect_v81`, ранжирует по состоятельности,
показывает на странице «Разведчик». ФАЗА 1 — ТОЛЬКО советует: ордера НЕ ставит, движок/боевой
воркер/execution НЕ трогает. Из секретов — лишь `DATABASE_URL` (общая Postgres); торговых ключей
нет вовсе → скаут архитектурно не может исполнять сделки (граница advisory).

План/аудит: docs/progress/scanner_mode.md.
"""
