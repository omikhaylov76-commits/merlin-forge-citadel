"""strategy.engine — движок-ЭТАЛОН V8.1 (порт из engine/, Веха 2).

Источник правды стратегии: detect_v81 (сигнал) + run_v8/run_v7 (машина выходов) +
port_lib (портфель: run_coin / walk_forward / compound). Закон parity: сигнал и
эталон выходов в live считаются ЭТИМ кодом, не переписанным вручную.

Подмодули импортируются лениво (по требованию: `from strategy.engine import port_lib`),
чтобы импорт самого пакета не тянул загрузку данных.
"""
